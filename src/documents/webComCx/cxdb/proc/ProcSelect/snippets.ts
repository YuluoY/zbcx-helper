export const snippets = {
  "cx-proc-select": {
    "prefix": "cx-proc-select",
    "body": [
      "<cx-proc-select",
      "  :value=\"${1:''}\"",
      "  @onChange=\"${2:onChange}\"",
      "/>"
    ],
    "description": "下拉选择流程 (如果是组，则返回的是组下面的全部流程ID)"
  }
}; 