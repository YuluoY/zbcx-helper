import { ComponentDoc } from '../../../../../types/tag-doc';
import IconDocument from './Icon';
import IconColorDocument from './IconColor';

export const documents: Record<string, ComponentDoc> = {
  Icon: IconDocument,
  IconColor: IconColorDocument
}; 