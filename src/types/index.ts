export interface BaseUrl {
    github: string;
    npm: string;
    official: string;
}

export type BaseLanguage = 'zh-CN' | 'en-US';

export interface OfficialWebsite {
    url: string;
    title: string;
} 